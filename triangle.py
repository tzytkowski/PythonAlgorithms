from re import A, S
import random
from tkinter import Y

class AorS(pick):
    def __init__(self, pick):
    self.a = a 
    self.s = s 
    self.x = random.randint(1, 100)
    self.y = random.randint(1, 100)
    def pick():
        picking = str(input("Pick A or S: ")):
        try:
            picking = picking.upper()
            if picking == "A":
                return x + y
            elif picking == "S":
                return x - Y
